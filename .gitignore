# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
.vscode/
.idea/

# Claude Code user settings (permissions, local config)
.claude/*
!.claude/skills/

# Environment files
.env
.env.local

# MCP configuration (references credential env vars)
.mcp.json

# Dev/test artifacts
archive/localhost*.json

# Lead magnets / freebies (attached via email, not public)
archive/downloads/

# Internal documentation (stored in Notion - see CLAUDE.md for workflow)
session-notes/
tasks/

# Sensitive account information (stored in Notion)
docs/accounts-inventory.md

# Internal Notion architecture documentation
docs/notion-system-architecture.md

# Local workspace identifiers (Notion DB/user IDs) — public repo, don't commit
.claude-local-ids.md

# Local Netlify folder
.netlify

# Internal tooling/handoffs — THIS REPO IS PUBLIC ON GITHUB and Netlify publishes its whole
# root (publish = "."), so anything committed here is world-readable twice over.
# Business/ops docs and scripts belong in the private trymebroh/IHH repo instead.
# (The Claude usage-sync handoff was committed here by mistake on 2026-07-31 — 12a5fc5 —
#  and now lives at tools/claude-usage-sync/ in trymebroh/IHH.)
internal/
